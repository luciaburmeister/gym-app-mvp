# ── Core ML / CV ──────────────────────────────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8-pose for real-time pose estimation
opencv-python>=4.8.0        # Webcam capture, frame processing, MJPEG encoding
numpy>=1.24.0               # Numerical operations
pandas>=2.0.0               # CSV loading and feature dataframes

# ── Machine Learning ───────────────────────────────────────────────────────────
scikit-learn>=1.3.0         # Random Forest classifiers, GridSearchCV, metrics
imbalanced-learn>=0.11.0    # SMOTE oversampling (fixes class imbalance at training time)

# ── Web server ─────────────────────────────────────────────────────────────────
flask>=3.0.0                # HTTP server, MJPEG video feed, /status and /force_exercise endpoints

# ── Training visualisation (only needed when running train_*.py scripts) ───────
matplotlib>=3.7.0           # Confusion matrices, feature importance charts
seaborn>=0.12.0             # Heatmap styling for confusion matrices
